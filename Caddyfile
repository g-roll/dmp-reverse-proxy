{
    # Global options
    admin off
    storage file_system {
        root /data
    }
}

# Globaler Handler für alle eingehenden Anfragen
* {
    tls {
        on_demand
    }
    
    reverse_proxy {host}:80
} 